/*import java.util.ArrayList;
import java.util.Scanner;
import java.util.Random;
import javax.swing.*;
import java.awt.*;
import java.awt.event.*;

class Craits extends JFrame{
  private static JFrame rulesScreen;
  private static JPanel rulesPane;
  //JButton rulesButton;

  private static JLabel lineOne;
  private static JLabel lineTwo;
  private static JLabel lineThree;
  JFrame frame;
  JPanel contentPane;
  JButton rollDie;
  JLabel dieFace;

  public Craits(){*/
    /*
    rulesScreen = new JFrame("The Rules of Craits by Albert Z and Jeffery T");
    //rulesPane = new JPanel();
    rulesScreen.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    rulesScreen.setSize(400,400);
    //rulesScreen.add(rulesPane, BorderLayout.CENTER);

    //rulesButton = new JButton("Read the rules!");
    //rulesButton.addActionListener(this);
    //rulesPane.add(rulesButton);

    //rulesPane.setBorder(BorderFactory.createEmptyBorder(30,30,10,30));
    //rulesPane.setBackground(Color.white);
    //rulesPane.setLayout(new GridLayout(0,1));  
    lineOne = new JLabel("Craits, a derivation of Crazy Eights plays very similarly to Uno, but a bit more simplified and crazy.");
    lineOne.setFont(new Font("Arial", Font.PLAIN, 8));
    lineOne.setBounds(10,20,80,25);
    lineOne.setForeground(Color.black);

    lineTwo = new JLabel("->Each round, you start off with 8 cards.\n->You can play cards that are the same suit or number as the current surface card\n->The goal is to get rid of all of the cards in your hand\n->1s can only be played in a count ->2 starts a special mode called the Count. In the count, you can only play 1s and 2s. You rack up count tokens (playing 2 adds 2 to the token count, and playing a 1 adds 1). At the end of the count (when one player cannot play any more 1s or 2s) the other player will be forced to draw cards according to the number of tokens accumulated\n->4 skips turn\n->5 makes all other players draw a card [since this is a 1v1 situation, this just makes the COM draw a card]\n->6 gives you a second turn [This is essentially a skip]\n->7 makes the next player draw a card\n->8 is a wild card\n->9 is a \"semiwild\" card, meaning it can only change to suits of the same color\n->10 reverses the direction [but in a 1v1 situation basically just skips]");
    lineTwo.setFont(new Font("Arial", Font.PLAIN, 8));
    lineTwo.setBounds(10,20,80,25);
    lineTwo.setForeground(Color.black);

    lineThree = new JLabel("->In craits, if you are told to draw a card, you actually do it after your turn is over. As a result, they're really powerful in preventing wins\n->No, you can't stack cards in craits");
    lineThree.setFont(new Font("Arial", Font.PLAIN, 8));
    lineThree.setBounds(10,20,80,25);
    lineThree.setForeground(Color.black);

    rulesScreen.add(lineOne);
    rulesScreen.add(lineTwo);
    rulesScreen.add(lineThree);

    //rulesScreen.add(rulesPane, BorderLayout.CENTER);
    rulesScreen.pack();
    rulesScreen.setVisible(true);
*/

/*
    
  }
}*/